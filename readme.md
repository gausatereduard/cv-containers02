# Лабораторная работа

## IWNO2: Виртуальный сервер

- Gausater Eduard, I2402;
- 15.02.2026;
- Виртуализация операционных систем (на примере ОС Debian) и настройка виртуального HTTP сервера (LAMP);

### Установка и запуск проекта

1. В консоли создайте в папке containers02 образ диска для виртуальной машины размером 8 ГБ, формата qcow2, используя утилиту qemu-img:
    `qemu-img create -f qcow2 debian.qcow2 8G`
2. Установите ОС Debian на виртуальную машину. Для этого выполните команду:
    `qemu-system-x86_64 -hda debian.qcow2 -cdrom dvd/debian.iso -boot d -m 2G`

    > Образ скачал отсюда [get.debian.org](https://get.debian.org/images/archive/12.13.0/amd64/iso-cd/)

3. При установке используйте следующие параметры:
    Имя компьютера: `debian`;
    Хостовое имя: `debian.localhost`;
    Имя пользователя: `user`;
    Пароль пользователя: `password`;
4. Для повторного запуска виртуальной машины выполните команду:
    `qemu-system-x86_64 -hda debian.qcow2 -m 2G -smp 2 -device e1000,netdev=net0 -netdev user,id=net0,hostfwd=tcp::1080-:80,hostfwd=tcp::1022-:22`

    Чтобы подключиться по SSH с основного хоста, используем `ssh user@localhost -p 1022`
5. Установите LAMP в виртуальной машине. Для этого переключитесь на суперпользователя и выполните команды:

    ```bash
    su
    apt update -y
    apt install -y apache2 php libapache2-mod-php php-mysql mariadb-server mariadb-client unzip
    ```

    > Какое назначение установленных пакетов?

    - apache2 - Веб-сервер. Принимает HTTP/S-запросы;
    - php - Без него Apache не смог бы обрабатывать .php файлы;
    - libapache2-mod-php - позволяет Apache выполнять PHP-код внутри себя;
    - php-mysql - Расширение PHP для работы с MySQL/MariaDB;
    - mariadb-server - Сервер базы данных MariaDB;
    - mariadb-client - Клиент для подключения к MariaDB;
    - unzip - Утилита для распаковки ZIP-архивов;
6. Скачайте СУБД PhpMyAdmin:
    `wget https://files.phpmyadmin.net/phpMyAdmin/5.2.2/phpMyAdmin-5.2.2-all-languages.zip`
7. Скачайте CMS WordPress:
    `wget https://wordpress.org/latest.zip`
8. Проверьте наличие файлов командой `ls -l` и Распакуйте скачанные файлы в папки:
    1. СУБД PhpMyAdmin ==> `/var/www/phpmyadmin`;
    2. CMS WordPress ==> `/var/www/wordpress`;

    ```bash
    mkdir /var/www
    unzip phpMyAdmin-5.2.2-all-languages.zip
    mv phpMyAdmin-5.2.2-all-languages /var/www/phpmyadmin
    unzip latest.zip
    mv wordpress /var/www/wordpress
    ```

9. Создайте через командную строку для CMS базу данных wordpress_db и пользователя базы данных с вашим именем:
    `mysql -u root`

    ```sql
    CREATE DATABASE wordpress_db;
    CREATE USER 'user'@'localhost' IDENTIFIED BY 'password';
    GRANT ALL PRIVILEGES ON wordpress_db.* TO 'user'@'localhost';
    FLUSH PRIVILEGES;
    EXIT;
    ```

10. В папке `/etc/apache2/sites-available` создайте файл `01-phpmyadmin.conf`
    `nano /etc/apache2/sites-available/01-phpmyadmin.conf`

    ```apacheconf
    <VirtualHost *:80>
        ServerAdmin webmaster@localhost
        DocumentRoot "/var/www/phpmyadmin"
        ServerName phpmyadmin.localhost
        ServerAlias www.phpmyadmin.localhost
        ErrorLog "/var/log/apache2/phpmyadmin.localhost-error.log"
        CustomLog "/var/log/apache2/phpmyadmin.localhost-access.log" common
    </VirtualHost>
    ```

    В папке `/etc/apache2/sites-available` создайте файл `02-wordpress.conf`

    `nano /etc/apache2/sites-available/02-wordpress.conf`

    ```apacheconf
    <VirtualHost *:80>
        ServerAdmin webmaster@localhost
        DocumentRoot "/var/www/wordpress"
        ServerName wordpress.localhost
        ServerAlias www.wordpress.localhost
        ErrorLog "/var/log/apache2/wordpress.localhost-error.log"
        CustomLog "/var/log/apache2/wordpress.localhost-access.log" common
    </VirtualHost>
    ```

11. Зарегистрируйте конфигурации, выполнив команды:
    `/usr/sbin/a2ensite 01-phpmyadmin`
    `/usr/sbin/a2ensite 02-wordpress`

12. Добавьте в файл /etc/hosts строки:
    `127.0.0.1 phpmyadmin.localhost`
    `127.0.0.1 wordpress.localhost`

=-=-=-=-=

### Тестирование

1. В открывшемся консольном окне выполните команду `uname -a`.
    > Что выводится на экране в результате выполнения данной команды?

    ```bash
    root@debian:~# uname -a
    Linux debian 6.1.0-43-amd64 #1 SMP PREEMPT_DYNAMIC Debian 6.1.162-1 (2026-02-08) x86_64 GNU/Linux
    ```

2. Перезагрузите Apache Web Server.
    > Как перезагрузить Apache Web Server?
    `systemctl restart apache2`

3. В браузере проверьте доступность сайтов <http://wordpress.localhost:1080> и <http://phpmyadmin.localhost:1080>. Завершите установку сайтов.

    Всё доступно, шеф 🫡

### Контрольные вопросы

- Каким образом можно скачать файл в консоли при помощи утилиты wget?

    Используем утилиту с указанием ссылки. Дополнительно можно прописать флаг `-O`, чтобы указать в какой файл или директорию сохранить скачанный файл: `wget <url> [-O <filename>]`

- Зачем необходимо создавать для каждого сайта свою базу и своего пользователя?

    1. Для безопасности, скромпрометированные данные от одного сайта - не касаются других баз;
    2. Для корректной работы сайтов. Один сайт может очистить базу перед началом или во время использования;
    3. Логически правильно;

- Как поменять доступ к системе управления БД на порт 1234?

    1. Создаем новый конфиг файл `/etc/mysql/conf.d/port.cnf`;
    2. Прописываем новый порт в конфиге:

    ```conf
    [mysqld]
    port = 3307
    ```

- Какие преимущества, с вашей точки зрения, даёт виртуализация?

    Запуск нескольких виртуальных машин с разными ОС. Простота и удобство бэкапа данных и переноса на другой сервер

- Для чего необходимо устанавливать время / временную зону на сервере?

    Логи, cron, работа баз данных, удобство для админа

- Сколько места занимает установленная вами ОС (виртуальный диск) на хостовой машине?

    ОС занимает ≈2.2G на основе данных с `df -h`

- Какие есть рекомендации по разбиению диска для серверов? Почему рекомендуется так разбивать диск?

    Можно разбить диск:
  - `/boot` загрузчик и ядро
  - `/` система и программы
  - `/var`  логи, базы, веб
  - `/home` директория пользователей
  - `swap`  подкачка

    Всё для изоляции, безопасности, простоты бэкапа
